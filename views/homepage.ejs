<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            background-color: #f4f4f4;
        }

        nav {
            background-color: #333;
            padding: 10px 0;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .logo {
            margin-left: 20px;
        }

        nav a {
            color: #fff;
            text-decoration: none;
            padding: 10px 20px;
            margin: 0 10px;
            border-radius: 5px;
            transition: background-color 0.3s;
            font-weight: bold;
            font-size: 18px;
        }

        nav a:hover {
            background-color: #555;
        }

        .logout-form button {
            background-color: #333;
            color: #fff;
            border: none;
            padding: 10px 20px;
            border-radius: 5px;
            cursor: pointer;
            font-weight: bold;
            font-size: 18px;
        }

        .logout-form button:hover {
            background-color: #555;
        }

        .container {
            text-align: center;
            margin-top: 50px;
        }

        h1 {
            color: #333;
        }

        .buttons {
            display: flex;
            flex-direction: column;
            gap: 20px;
            justify-content: center;
            align-items: center;
            margin-top: 20px;
        }

        .buttons-row {
            display: flex;
            gap: 20px;
        }

        .buttons a {
            color: #fff;
            background-color: #333;
            text-decoration: none;
            padding: 20px;
            border-radius: 5px;
            transition: background-color 0.3s;
            font-weight: bold;
            font-size: 18px;
            text-align: center;
        }

        .buttons a:hover {
            background-color: #555;
        }
    </style>
</head>
<body>
    <nav>
        <div class="logo">
            <a href="/homepage">Home</a>
        </div>
        <div class="logout-form">
            <form action="/logout" method="post" style="display: inline;">
                <button type="submit">Logout</button>
            </form>
        </div>
    </nav>
    <div class="container">
        <h1>Android Device Manager</h1>
        <div class="buttons">
            <div class="buttons-row">
                <a href="/pairdevice">Wireless Pairing</a>
                <a href="/wiredpairdevice">Wired Pairing</a>
            </div>
            <div class="buttons-row">
                <a href="#" onclick="document.getElementById('connectedDevicesForm').submit();return false;">Connected Devices</a>
            </div>
            <div class="buttons-row">
                <a href="/screenmirror">Mirror Device</a>
                <a href="/deviceDetails">Device Details</a>
                <a href="/openADBshell">Open Shell</a>
            </div>
        </div>
        <!-- Hidden form to submit when "Show Connected Devices" is clicked -->
        <form action="/connecteddevices" method="post" id="connectedDevicesForm" style="display: none;"></form>
    </div>
</body>
</html>
